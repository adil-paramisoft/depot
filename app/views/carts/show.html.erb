<% @cart.line_items.each do |item| %>
    <%= item.quantity %> × <%=item.price%> <%= item.product.title %> <br>
    <%= button_to "delete",item, :method => :delete %>
<% end %>

<%= button_to 'Empty cart', @cart, :method => :delete, :confirm => 'Are you sure?' %> 